# ---- Web layer -------------------------------------------------------------
fastapi==0.121.2
uvicorn[standard]==0.41.0
python-multipart==0.0.20
# 2.13.5, not 2.12.4: google-genai (below) requires a newer pydantic-core
# than 2.12.4 ships, and pip's own resolver confirmed 2.13.5 as the correct
# minimum bump -- the full test suite passes unchanged on it.
pydantic==2.13.5
pydantic-settings==2.13.1

# ---- Persistence -----------------------------------------------------------
SQLAlchemy==2.0.45

# ---- Audio -----------------------------------------------------------------
# numpy is pinned <2 because chatterbox-tts pins it there for Python < 3.13.
numpy==1.26.4
soundfile==0.13.1

# ---- Voice cloning engine --------------------------------------------------
# Chatterbox pulls torch, torchaudio, transformers, librosa, s3tokenizer and
# resemble-perth itself. Install a CUDA build of torch first if you want GPU:
#   pip install torch==2.6.0 torchaudio==2.6.0 --index-url https://download.pytorch.org/whl/cu124
# Model *weights* are never vendored -- see scripts/download_model.py.
chatterbox-tts==0.1.7

# ---- Fairy-tale generation (multi-provider) --------------------------------
# Text generation only -- no audio -- for the "Create Fairy Tale" feature.
# Every provider here is optional at runtime (app/services/ai_providers/):
# with none configured, story generation returns a clear 503 rather than a
# stack trace, and Text to Speech / voice cloning are entirely unaffected.
anthropic==1.5.0
openai==3.13.0
google-genai==2.23.0
# Ollama provider talks to its REST API directly (no SDK needed); httpx
# comes in transitively via google-genai but is pinned explicitly since
# ai_providers/ollama_provider.py imports it directly.
httpx==0.28.1
