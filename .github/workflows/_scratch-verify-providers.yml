name: Scratch verify AI providers deploy

on:
  workflow_dispatch:
    inputs:
      url:
        description: Backend base URL to check
        type: string
        default: https://voice-clone-2nhg.onrender.com

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Wait for the new /ai/providers endpoint to come up
        run: |
          url="${{ inputs.url }}/api/v1/ai/providers"
          echo "Polling $url (Render Docker rebuild can take several minutes)..."
          for i in $(seq 1 40); do
            status=$(curl -sS -o /tmp/body.json -w '%{http_code}' -m 20 "$url" || echo "000")
            echo "attempt $i: HTTP $status"
            if [ "$status" = "200" ]; then
              echo "=== response ==="
              cat /tmp/body.json
              echo ""
              exit 0
            fi
            sleep 15
          done
          echo "::error::Endpoint never came up (still old backend, or deploy failed)"
          cat /tmp/body.json 2>/dev/null || true
          exit 1

      - name: Confirm existing endpoints still work (no regression)
        if: always()
        run: |
          echo "=== /api/v1/voices/defaults ==="
          curl -sS -m 20 "${{ inputs.url }}/api/v1/voices/defaults" | head -c 500
          echo ""
          echo "=== /api/v1/system/info ==="
          curl -sS -m 20 "${{ inputs.url }}/api/v1/system/info" | head -c 1000
